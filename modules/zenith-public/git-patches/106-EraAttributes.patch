diff --git a/src/map/entities/battleentity.cpp b/src/map/entities/battleentity.cpp
index a7b06338cb..edeca68530 100644
--- a/src/map/entities/battleentity.cpp
+++ b/src/map/entities/battleentity.cpp
@@ -921,6 +921,13 @@ uint16 CBattleEntity::CHR()
     return std::clamp(stats.CHR + m_modStat[Mod::CHR], 0, 999);
 }
 
+// Era-style attribute scaling for levels 37-75
+static float getEraScaling(int level, float startValue, float endValue) {
+    if (level < 37) return startValue;
+    if (level >= 75) return endValue;
+    return startValue + (endValue - startValue) * ((level - 37) / 38.0f);
+}
+
 uint16 CBattleEntity::ATT(SLOTTYPE slot)
 {
     TracyZoneScoped;
@@ -937,15 +944,19 @@ uint16 CBattleEntity::ATT(SLOTTYPE slot)
     }
     else if (weapon && weapon->isTwoHanded()) // 2-handed weapon
     {
-        strMultiplier = 1.0;
+        strMultiplier = 0.75f;
     }
     else if (weapon && weapon->isHandToHand()) // H2H Weapon
     {
-        strMultiplier = 0.75;
+        strMultiplier = getEraScaling(GetMLevel(), 0.5f, 0.625f);
     }
-    else if (slot == SLOT_MAIN || slot == SLOT_RANGED || slot == SLOT_AMMO) // 1-handed weapon in main slot, Ranged or ammo weapon.
+    else if (slot == SLOT_MAIN) // 1-handed weapon in main slot
     {
-        strMultiplier = 1.0;
+        strMultiplier = getEraScaling(GetMLevel(), 0.5f, 0.75f);
+    }
+    else if (slot == SLOT_RANGED || slot == SLOT_AMMO) // Ranged or ammo weapon
+    {
+        strMultiplier = getEraScaling(GetMLevel(), 0.5f, 0.625f);
     }
 
     if (settings::get<bool>("main.USE_PRE_2013_STR_MULTIPLIER"))
@@ -1012,7 +1023,7 @@ uint16 CBattleEntity::RATT(uint16 bonusAtt)
 
     if (objtype == TYPE_PC)
     {
-        strMultiplier = 1.0;
+        strMultiplier = getEraScaling(GetMLevel(), 0.5f, 0.625f);
         auto* weapon  = dynamic_cast<CItemWeapon*>(m_Weapons[SLOT_RANGED]);
 
         // Return 0 if ranged weapon but no ammo
@@ -1145,7 +1156,7 @@ uint16 CBattleEntity::RACC(uint16 bonusAcc)
         RACC += getMod(Mod::RACC);
         RACC += bonusAcc;
         RACC += battleutils::GetRangedAccuracyBonuses(this);
-        RACC += std::floor(AGI() * 3 / 4);
+        RACC += (int16)(AGI() * getEraScaling(GetMLevel(), 0.5f, 0.75f));
     }
     else if (objtype & TYPE_PET && static_cast<CPetEntity*>(this)->getPetType() == PET_TYPE::AUTOMATON)
     {
@@ -1263,10 +1274,10 @@ uint16 CBattleEntity::ACC(uint8 attackNumber, uint16 offsetAccuracy)
         uint32_t skillLevel = GetSkill(skill) + iLvlSkill;
         ACC                 = GetAccFromSkill(skillLevel);
 
-        float dexMultiplier = settings::get<bool>("main.USE_PRE_2013_DEX_MULTIPLIER") ? 0.50f : 0.75f;
+        float dexMultiplier = attackNumber == 1 && skill != SKILL_HAND_TO_HAND ? 0.50f : getEraScaling(GetMLevel(), 0.5f, 0.75f);
         if (auto* weapon = dynamic_cast<CItemWeapon*>(m_Weapons[SLOT_MAIN]); weapon && weapon->isTwoHanded())
         {
-            ACC += std::floor(DEX() * dexMultiplier);
+            ACC += std::floor(DEX() * 0.75f);
             ACC += m_modStat[Mod::TWOHAND_ACC];
         }
         else
@@ -1363,7 +1374,7 @@ uint16 CBattleEntity::DEF()
     // https://wiki.ffo.jp/html/35712.html
     // https://forum.square-enix.com/ffxi/threads/51154-Aug.-3-2016-%28JST%29-Version-Update?p=583669&viewfull=1#post583669
     int32 DEF       = 8 + m_modStat[Mod::DEF];
-    float vitFactor = 1.5f;
+    float vitFactor = getEraScaling(GetMLevel(), 0.5f, 1.0f);
 
     if (this->objtype == TYPE_MOB)
     {
