-- -----------------------------------
-- xi.item additions
--
-- Add enums we need that aren't needed in LSB
-- -----------------------------------

require('modules/module_utils')

local m = Module:new('enum_item')

-- Vendor overrides
xi.item.KARUGO_CLAY = 2563

return m
